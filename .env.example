# Optional future backend URL. Public values are visible in the application bundle.
EXPO_PUBLIC_API_BASE_URL=
# v0.1.1 runs locally and does not connect to any provider.
# Real provider secrets belong ONLY in a backend .env, never EXPO_PUBLIC_* or app.json.
