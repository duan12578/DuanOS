# Optional future backend URL. Public values are visible in the application bundle.
EXPO_PUBLIC_API_BASE_URL=
# v0.2.0 uses same-origin /api routes. Leave this empty for the standard deployment.
# Provider secrets belong only in Cloudflare settings or local .dev.vars.
